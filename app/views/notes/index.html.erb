<h1>My Notes</h1>

<%= simple_form_for @new_note, html: {id: "note_form"} do |f| %>
  <%= f.input :title, label: false, placeholder: "Enter a Note" %>
  <%= f.submit "Add Note"  %>

<% end %>

<div id="notes">
  <% @notes.each do |note| %>
    <li><%= note.title %></li>
  <% end %>
</div>